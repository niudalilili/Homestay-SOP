<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tanyde.mapper.EmployeeMapper">

    <insert id="addEmployeeRole">
        insert into employee_role(employee_id, role_id)
        values (#{id}, #{roleId})
    </insert>

    <select id="getRoleIdById" resultType="java.lang.Long">
        select role_id
        from employee_role
        where employee_id = #{employeeId}
    </select>

    <update id="updateEmployeeRole">
        update employee_role
        set role_id = #{roleId}
        where employee_id = #{employeeId}
    </update>

    <select id="countByRoleId" resultType="java.lang.Integer">
        select count(*)
        from employee_role
        where role_id = #{id}
    </select>

    <delete id="deleteEmployeeRoleById">
        delete from employee_role
        where employee_id = #{id}
    </delete>

    <select id="getByName" resultType="com.tanyde.entity.LoginPO.Employee">
        select id, username, name, avatar,
               password, openid, login_type, status,
               create_time, update_time, create_user, update_user
        from employee
        where name = #{name}
        limit 1
    </select>

    <select id="pageQuery" resultType="com.tanyde.vo.EmployeePageVO">
        select
        e.id,e.username,e.name,e.status,e.avatar,
        r.name as role_name
        from employee e
        left join employee_role er ON e.id=er.employee_id
        left join role r ON er.role_id=r.id
        <where>
            <if test="dto.name != null and dto.name !=''">
                                and e.name like concat('%',#{dto.name},'%')</if>
            <if test="dto.username != null and dto.username !=''">
                                and e.username like concat('%',#{dto.username},'%')</if>
            <if test="dto.status != null">
                                and e.status=#{dto.status}</if>
        </where>
        order by e.id ASC
    </select>



</mapper>
